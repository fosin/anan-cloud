spring:
  zipkin:
    rabbitmq:
      queue: zipkin
  jpa:
    show-sql: true
  datasource:
    url: jdbc:mysql://192.168.137.155:3306/mpi?useUnicode=true&characterEncoding=utf-8
    username: cdp
    password: local
    druid:
      driver-class-name: com.mysql.jdbc.Driver
#    platform: mysql
#    continue-on-error: true
  redis:
    host: 192.168.2.242
    port: 6379
    password: dev
management:
  security:
    enabled: false
security:
  oauth2:
    resource:
      filter-order: 3
      loadBalanced: true #这个参数在OAuth2LoadBalancerClientAutoConfiguration可以找到，设置这个才可以使用eureka的服务名，配合loadBalancerInterceptor bean。
      user-info-uri: http://cdp-authserver/oauth/principal
      prefer-token-info: false
      service-id: cdp-platformserver
cdp:
  swagger:
    enabled: true
    title: ${spring.application.name}
    description: ${info.description}
    version: ${info.version}
    base-package: com.github.fosin.cdp.platform
    authorization:
      name: Oauth2.0 Authorization
      keyName: Authorization
    contact:
      name: fosin
      email: 28860823@qq.com
  oauth2:
    disablecsrf: true
    disableHttpBasic: true
    blackWhiteList:
    - path: /**/v2/api-docs